import { CardExpirationDirective } from './card-expiration.directive';

describe('CardExpirationDirective', () => {
  it('should create an instance', () => {
    const directive = new CardExpirationDirective();
    expect(directive).toBeTruthy();
  });
});
